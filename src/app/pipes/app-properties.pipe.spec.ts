import { AppPropertiesPipe } from './app-properties.pipe';

describe('AppPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new AppPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
